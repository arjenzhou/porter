plugins {
    id 'java-library'
}

dependencies {
    api project(':api')
    api project(':transfer')
    implementation project(':common')
    implementation project(':transfer:transfer-jdbc')
    implementation project(':transfer:transfer-http')
}